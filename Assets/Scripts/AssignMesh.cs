using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AssignMesh : MonoBehaviour {

	// Use this for initialization
	void Start () {

//		Material newMat = Resources.Load("PAN2", typeof(Material)) as Material;
//		GameObject.FindWithTag("World").GetComponent<Renderer>().material.mainTexture = Resources.Load("Arbeitszimmer84874369", typeof(Texture)) as Texture;

	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
